'use strict';
const GOOGLE_CLIENT_ID='287824088654-d53qa4ann3a1hq3uj917htsc0c0ormjr.apps.googleusercontent.com';
const GOOGLE_API_KEY='AIzaSyB5YIk5RUPw0E518259rIc_lN5QhPs3fFM';
const GOOGLE_APP_ID='287824088654';
const SCOPE='https://www.googleapis.com/auth/drive.file https://www.googleapis.com/auth/spreadsheets https://www.googleapis.com/auth/userinfo.profile';
const LOG_SHEET='_InstaCheckList_Log', ITEM_ID_HEADER='_InstaCheckList_ID';
const $=id=>document.getElementById(id);
const dom={setup:$('setup'),scanner:$('scanner'),workspace:$('workspace'),connectBtn:$('connectBtn'),selectSheetBtn:$('selectSheetBtn'),changeSheetBtn:$('changeSheetBtn'),setupStatus:$('setupStatus'),video:$('video'),cameraMessage:$('cameraMessage'),serialInput:$('serialInput'),openDeviceBtn:$('openDeviceBtn'),cameraToggleBtn:$('cameraToggleBtn'),newScanBtn:$('newScanBtn'),serialLabel:$('serialLabel'),syncBar:$('syncBar'),progress:$('progress'),checklist:$('checklist'),emptyItems:$('emptyItems'),toast:$('toast')};
const state={token:'',expires:0,tokenClient:null,pickerReady:false,spreadsheetId:'',sheetTitle:'',items:[],logs:new Map(),serial:'',userName:'Googleユーザー',stream:null,reader:null,scanning:false,saving:false,pendingReload:false};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function toast(message){dom.toast.textContent=message;dom.toast.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>dom.toast.classList.remove('show'),2600)}
function status(text,kind='ready'){dom.syncBar.textContent=text;dom.syncBar.dataset.kind=kind}
function setupStatus(text,error=false){dom.setupStatus.textContent=text;dom.setupStatus.classList.toggle('error',error)}
function initGoogle(){if(window.google?.accounts?.oauth2&&!state.tokenClient)state.tokenClient=google.accounts.oauth2.initTokenClient({client_id:GOOGLE_CLIENT_ID,scope:SCOPE,callback:()=>{}});if(window.gapi&&!state.pickerReady)gapi.load('picker',()=>state.pickerReady=true)}
function authorize(){return new Promise((resolve,reject)=>{initGoogle();if(state.token&&Date.now()<state.expires-60000)return resolve();if(!state.tokenClient)return reject(new Error('Google認証の読み込み中です。数秒後に再試行してください。'));state.tokenClient.callback=r=>{if(r.error)return reject(new Error(r.error));state.token=r.access_token;state.expires=Date.now()+(Number(r.expires_in)||3600)*1000;resolve()};state.tokenClient.requestAccessToken({prompt:state.token?'':'consent'})})}
async function api(url,options={}){await authorize();const response=await fetch(url,{...options,headers:{Authorization:`Bearer ${state.token}`,...(options.headers||{})}});if(!response.ok){let detail='';try{detail=(await response.json()).error?.message||''}catch{}throw new Error(detail||`Google API error (${response.status})`)}return response.status===204?{}:response.json()}
async function loadUser(){try{const u=await api('https://www.googleapis.com/oauth2/v3/userinfo');state.userName=u.name||u.email||state.userName}catch{}}
async function chooseSheet(){try{await authorize();initGoogle();if(!state.pickerReady)throw new Error('Google Pickerを準備中です。数秒後に再試行してください。');const view=new google.picker.DocsView().setMimeTypes('application/vnd.google-apps.spreadsheet').setSelectFolderEnabled(false);new google.picker.PickerBuilder().addView(view).setOAuthToken(state.token).setDeveloperKey(GOOGLE_API_KEY).setAppId(GOOGLE_APP_ID).setOrigin(location.origin).setCallback(async data=>{if(data.action!==google.picker.Action.PICKED)return;state.spreadsheetId=data.docs[0].id;localStorage.setItem('instachecklist-sheet-id',state.spreadsheetId);setupStatus('Spreadsheetを読み込んでいます…');try{await Promise.all([loadUser(),loadWorkbook()]);dom.setup.hidden=true;dom.scanner.hidden=false;setupStatus('接続済み');startCamera()}catch(e){setupStatus(e.message,true)}}).build().setVisible(true)}catch(e){setupStatus(e.message,true)}}
const sheetsUrl=(path='')=>`https://sheets.googleapis.com/v4/spreadsheets/${encodeURIComponent(state.spreadsheetId)}${path}`;
async function batchUpdate(requests){return api(sheetsUrl(':batchUpdate'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({requests})})}
async function valuesGet(range){return api(sheetsUrl(`/values/${encodeURIComponent(range)}?majorDimension=ROWS`))}
async function valuesUpdate(range,values){return api(sheetsUrl(`/values/${encodeURIComponent(range)}?valueInputOption=RAW`),{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({values})})}
async function loadWorkbook(){const meta=await api(sheetsUrl('?fields=sheets.properties'));const visible=meta.sheets.find(s=>s.properties.title!==LOG_SHEET&&!s.properties.hidden);if(!visible)throw new Error('作業用シートが見つかりません。');state.sheetTitle=visible.properties.title;await ensureLogSheet(meta.sheets);await loadItems();if(state.serial)await loadLogs();}
async function ensureLogSheet(sheets){if(sheets.some(s=>s.properties.title===LOG_SHEET))return;await batchUpdate([{addSheet:{properties:{title:LOG_SHEET,hidden:true}}}]);await valuesUpdate(`${LOG_SHEET}!A1:H1`,[['シリアル番号','項目ID','完了日時','ユーザー名','固有値','作業メモ','大項目','項目名']])}
function uuid(){return crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random().toString(36).slice(2)}`}
async function loadItems(){const data=await valuesGet(`${state.sheetTitle}!A:J`);const rows=data.values||[];const expected=['有効/無効','順番','大項目','中項目/参照値','小項目/参照値','作業指示補足','作業確認','固有値(MACアドレスなど)','作業メモ'];if(!rows[0]||expected.some((h,i)=>String(rows[0][i]||'').trim()!==h))throw new Error('タイトル行（A1:I1）が指定の形式と一致しません。');const idWrites=[];state.items=rows.slice(1).map((r,index)=>{let id=r[9];if(!id){id=uuid();idWrites.push({range:`${state.sheetTitle}!J${index+2}`,values:[[id]]})}return{row:index+2,id,enabled:/^(有効|true|1|yes|on|✓)$/i.test(String(r[0]||'').trim()),order:r[1]||index+1,major:r[2]||'',middle:r[3]||'',small:r[4]||'',instruction:r[5]||'',confirm:r[6]||'',uniqueLabel:r[7]||'',memoDefault:r[8]||''}}).filter(x=>x.enabled).sort((a,b)=>Number(a.order)-Number(b.order)||String(a.order).localeCompare(String(b.order),'ja'));if(idWrites.length)await api(sheetsUrl('/values:batchUpdate'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({valueInputOption:'RAW',data:idWrites})});if(rows[0][9]!==ITEM_ID_HEADER)await valuesUpdate(`${state.sheetTitle}!J1`,[[ITEM_ID_HEADER]])}
async function loadLogs(renderNow=true){const data=await valuesGet(`${LOG_SHEET}!A:H`);state.logs.clear();(data.values||[]).slice(1).forEach((r,index)=>{if(String(r[0])===state.serial)state.logs.set(String(r[1]),{row:index+2,date:r[2]||'',user:r[3]||'',unique:r[4]||'',memo:r[5]||''})});if(renderNow)render()}
function render(){let lastMajor='';dom.checklist.innerHTML=state.items.map(item=>{const log=state.logs.get(item.id);const heading=item.major!==lastMajor?`<h2 class="group-title">${esc(item.major||'チェック項目')}</h2>`:'';lastMajor=item.major;return`${heading}<article class="item ${log?'done':''}" data-id="${esc(item.id)}"><div class="item-top"><button class="check-btn" aria-label="完了を切り替える">✓</button><div><span class="order">${esc(item.order)}</span><h3>${esc(item.small||item.middle||'名称未設定')}</h3>${item.middle&&item.small?`<p class="middle">${esc(item.middle)}</p>`:''}${item.instruction?`<p class="instruction">${esc(item.instruction)}</p>`:''}</div></div><div class="fields">${item.uniqueLabel?`<label>固有値 <span class="required">必須</span><input class="unique" value="${esc(log?.unique||'')}" placeholder="${esc(item.uniqueLabel)}"></label>`:''}<label>作業メモ ${item.uniqueLabel?'<span class="required">必須</span>':''}<textarea class="memo" rows="2" placeholder="作業内容を入力">${esc(log?.memo||item.memoDefault)}</textarea></label></div>${log?`<p class="audit">✓ ${esc(log.date)}　${esc(log.user)}</p>`:''}</article>`}).join('');dom.emptyItems.hidden=state.items.length>0;dom.checklist.querySelectorAll('.check-btn').forEach(b=>b.addEventListener('click',()=>toggleItem(b.closest('.item'))));const done=state.items.filter(i=>state.logs.has(i.id)).length;dom.progress.firstElementChild.style.width=`${state.items.length?done/state.items.length*100:0}%`}
function localDate(){return new Intl.DateTimeFormat('ja-JP',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false,timeZone:'Asia/Tokyo'}).format(new Date())}
async function toggleItem(el){if(state.saving)return;const id=el.dataset.id;const unique=el.querySelector('.unique')?.value.trim()||'',memo=el.querySelector('.memo')?.value.trim()||'';state.saving=true;status('シートの変更を確認中…','saving');try{await loadItems();await loadLogs(false);const item=state.items.find(x=>x.id===id),existing=state.logs.get(id);if(!item){render();throw new Error('この項目はSpreadsheet側で削除または無効化されました。')}if(!existing&&item.uniqueLabel&&(!unique||!memo)){render();toast('固有値と作業メモを入力してから完了してください。');return}status('保存中…','saving');if(existing){await batchUpdate([{deleteDimension:{range:{sheetId:await logSheetId(),dimension:'ROWS',startIndex:existing.row-1,endIndex:existing.row}}}]);state.logs.delete(id)}else{const date=localDate();await api(sheetsUrl(`/values/${encodeURIComponent(LOG_SHEET+'!A:H')}:append?valueInputOption=RAW&insertDataOption=INSERT_ROWS`),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({values:[[state.serial,id,date,state.userName,unique,memo,item.major,item.small||item.middle]]})});state.logs.set(id,{date,user:state.userName,unique,memo})}await loadItems();await loadLogs();status(`保存済み · ${new Date().toLocaleTimeString('ja-JP')}`,'saved')}catch(e){status(`保存失敗: ${e.message}`,'error');toast(e.message||'保存できませんでした。')}finally{state.saving=false}}
async function logSheetId(){const m=await api(sheetsUrl('?fields=sheets.properties'));return m.sheets.find(s=>s.properties.title===LOG_SHEET).properties.sheetId}
async function openDevice(serial=dom.serialInput.value.trim()){if(!serial)return toast('シリアル番号を入力してください。');state.serial=serial;dom.serialLabel.textContent=serial;dom.scanner.hidden=true;dom.workspace.hidden=false;stopCamera();status('最新データを読み込み中…');try{await loadItems();await loadLogs();status('準備完了')}catch(e){status(e.message,'error')}}
async function startCamera(){if(state.stream)return;try{state.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280}},audio:false});dom.video.srcObject=state.stream;await dom.video.play();dom.cameraMessage.hidden=true;state.scanning=true;scanLoop()}catch{dom.cameraMessage.textContent='カメラを利用できません。手入力してください。'}}
function stopCamera(){state.scanning=false;state.stream?.getTracks().forEach(t=>t.stop());state.stream=null;dom.video.srcObject=null}
async function scanLoop(){if(!state.scanning)return;try{if('BarcodeDetector'in window){const detector=scanLoop.detector||(scanLoop.detector=new BarcodeDetector({formats:['code_128','code_39','ean_13','ean_8','qr_code','data_matrix']}));const codes=await detector.detect(dom.video);if(codes[0]?.rawValue){dom.serialInput.value=codes[0].rawValue;openDevice(codes[0].rawValue);return}}else if(window.ZXing){state.reader=state.reader||new ZXing.BrowserMultiFormatReader();const result=await state.reader.decodeOnceFromVideoElement(dom.video).catch(()=>null);if(result){openDevice(result.text);return}}}catch{}if(state.scanning)setTimeout(scanLoop,250)}
dom.connectBtn.addEventListener('click',chooseSheet);dom.selectSheetBtn.addEventListener('click',chooseSheet);dom.changeSheetBtn.addEventListener('click',chooseSheet);dom.openDeviceBtn.addEventListener('click',()=>openDevice());dom.serialInput.addEventListener('keydown',e=>{if(e.key==='Enter')openDevice()});dom.cameraToggleBtn.addEventListener('click',()=>state.stream?stopCamera():startCamera());dom.newScanBtn.addEventListener('click',()=>{state.serial='';state.logs.clear();dom.workspace.hidden=true;dom.scanner.hidden=false;dom.serialInput.value='';startCamera()});window.addEventListener('load',initGoogle);document.addEventListener('visibilitychange',()=>{if(document.hidden)stopCamera()});
